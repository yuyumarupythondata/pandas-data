#特定の文字列のn番目を削除したい場合
#slicingのn番目の文字は含まないという性質を使用している。
str = input()
print(str[:n] + str[n+1:])

#文字列更新(1文字ずつ拡大アルゴ)
str = input()
retult = ""
for i in range(len(str)):
    result = result + str[:i+1]
return result

#あやふやになりそうな箇所はコーディングして明確にする

#in 演算子（返り値True or False）
object in list
object not in list
"h" in "hello"
"h" not in "hello"
